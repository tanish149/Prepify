# Prepify

A recipe generating app built using React.js. It uses a huggingface API(mistral model) to generate the recipe and then return it.
The output is formatted using React-Markdown component for consistent and efficient display of the output.
